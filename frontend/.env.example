# Base URL of the FastAPI backend (no trailing slash). Defaults to localhost:8000.
VITE_API_BASE=http://localhost:8000

# Google OAuth client ID (public — safe to expose in the frontend).
VITE_GOOGLE_CLIENT_ID=
